export enum Routes {
  AUTH = 'auth',
  USER = 'user',
  CONVERSATIONS = 'conversations',
  MESSAGE = 'messages',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USER = 'USER_SERVICE',
  CONVERSATIONS = 'CONVERSATIONS_SERVICE',
  MESSAGE = 'MESSAGES_SERVICE',
}
